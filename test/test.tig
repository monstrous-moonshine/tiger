let type a = int
    type b = {a: a, b: a}
    var a : b := nil
    var a : int := 3
    function fn1(a: int, a: b) : int = if a = 0 then 1 else fn2(a - 1, "abc")
    function fn2(a: int, a: string) : int = if a = 0 then 0 else fn1(a - 1, nil)
 in
    while 0 do break;
    break;
    a.field := record { a=1, b=2, c=3};
    b.arr := arr [1 + 2] of func(3, 6);
    let in end;
    my_fun(1, 2, 3)
end
